{% extends 'base.html.twig' %}

{% block title %}Spin&Sound | Vinilos{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
    async function toggleFavorito(viniloId) {
        try {
            const response = await fetch(`/favoritos/toggle/${viniloId}`, {
                method: 'POST',
                headers: {
                    'X-Requested-With': 'XMLHttpRequest'
                }
            });
            
            if (response.ok) {
                const data = await response.json();
                const btn = document.getElementById(`fav-btn-${viniloId}`);
                const icon = btn.querySelector('.material-symbols-outlined');
                
                if (data.status === 'added') {
                    icon.textContent = 'favorite';
                    btn.classList.add('bg-[#e00000]');
                    if (typeof showToast === 'function') {
                        showToast('Añadido a favoritos');
                    }
                } else {
                    icon.textContent = 'favorite_border';
                    btn.classList.remove('bg-[#e00000]');
                    if (typeof showToast === 'function') {
                        showToast('Eliminado de favoritos');
                    }
                }
            }
        } catch (error) {
            console.error('Error:', error);
        }
    }
    </script>
{% endblock %}

{% block body %}
<main class="container mx-auto px-4 py-8">
    <div class="grid grid-cols-1 lg:grid-cols-4 gap-8">

        <aside class="lg:col-span-1">
            <div class="sticky top-24 pt-4">
                {% include 'vinilo/_filters.html.twig' %}
            </div>
        </aside>

        <section class="lg:col-span-3">
            <div class="flex flex-wrap items-center justify-between gap-4 mb-6">
                <h1 class="text-2xl font-bold text-white">
                    {% if query or genero or artista or precio_max %}
                        Resultados filtrados
                        <span class="text-base font-normal text-on-surface-muted-dark">
                            ({{ vinilos|length }} {{ vinilos|length == 1 ? 'vinilo' : 'vinilos' }})
                        </span>
                    {% else %}
                        Catálogo de Vinilos
                        <span class="text-base font-normal text-on-surface-muted-dark">
                            ({{ vinilos|length }})
                        </span>
                    {% endif %}
                </h1>
            </div>

            {# Etiquetas de filtros activos #}
            {% if query or genero or artista or precio_max %}
                <div class="flex flex-wrap gap-2 mb-6">
                    {% if query %}
                        <span class="inline-flex items-center gap-1 rounded-full bg-primary/20 px-3 py-1 text-xs text-primary">
                            🔍 "{{ query }}"
                            <a href="{{ path('app_vinilo_index', {genero: genero, artista: artista, precio_max: precio_max, orden: orden}) }}" class="hover:text-white">✕</a>
                        </span>
                    {% endif %}
                    {% if genero %}
                        <span class="inline-flex items-center gap-1 rounded-full bg-primary/20 px-3 py-1 text-xs text-primary">
                            🎵 {{ genero }}
                            <a href="{{ path('app_vinilo_index', {q: query, artista: artista, precio_max: precio_max, orden: orden}) }}" class="hover:text-white">✕</a>
                        </span>
                    {% endif %}
                    {% if artista %}
                        {% for a in artistas %}
                            {% if (a.id ~ '') == artista %}
                                <span class="inline-flex items-center gap-1 rounded-full bg-primary/20 px-3 py-1 text-xs text-primary">
                                    👤 {{ a.nombre }}
                                    <a href="{{ path('app_vinilo_index', {q: query, genero: genero, precio_max: precio_max, orden: orden}) }}" class="hover:text-white">✕</a>
                                </span>
                            {% endif %}
                        {% endfor %}
                    {% endif %}
                    {% if precio_max %}
                        <span class="inline-flex items-center gap-1 rounded-full bg-primary/20 px-3 py-1 text-xs text-primary">
                            💶 Máx. {{ precio_max }} €
                            <a href="{{ path('app_vinilo_index', {q: query, genero: genero, artista: artista, orden: orden}) }}" class="hover:text-white">✕</a>
                        </span>
                    {% endif %}
                </div>
            {% endif %}

            {% if vinilos is not empty %}
                <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                    {% for vinilo in vinilos %}
                        <article class="group rounded-xl bg-surface-dark overflow-hidden shadow-lg hover:shadow-primary/20 transition-shadow border border-gray-800">
                            <a href="{{ path('app_vinilo_show', {id: vinilo.id}) }}" class="block">
                                <div class="aspect-square overflow-hidden bg-background-dark flex items-center justify-center">
                                    {% if vinilo.imagen %}
                                        <img src="{{ vinilo.imagen }}" alt="{{ vinilo.titulo }}" class="h-full w-full object-cover object-center">
                                    {% else %}
                                        <svg fill="currentColor" class="h-16 w-16 text-on-surface-muted-dark" viewBox="0 0 256 256">
                                            <path d="M128,24A104,104,0,1,0,232,128,104.11,104.11,0,0,0,128,24Zm0,192a88,88,0,1,1,88-88A88.1,88.1,0,0,1,128,216Zm40-96a40,40,0,1,1-40-40A40,40,0,0,1,168,120Zm-16,0a24,24,0,1,0-24,24A24,24,0,0,0,152,120Z"/>
                                        </svg>
                                    {% endif %}
                                </div>
                                <div class="p-4">
                                    <h3 class="font-semibold text-white text-sm leading-snug line-clamp-2">{{ vinilo.titulo }}</h3>
                                    {% if vinilo.artistas.first %}
                                        <a href="{{ path('app_vinilo_index', {artista: vinilo.artistas.first.id}) }}" class="mt-1 text-xs text-primary hover:text-[#e00000] hover:underline transition-colors block">{{ vinilo.artistas.first.nombre }}</a>
                                    {% endif %}
                                    {% if vinilo.generos.first %}
                                        <a href="{{ path('app_vinilo_index', {genero: vinilo.generos.first.nombre}) }}" class="mt-1 text-xs text-on-surface-muted-dark hover:text-[#e00000] hover:underline transition-colors block">{{ vinilo.generos.first.nombre }}</a>
                                    {% endif %}
                                    <p class="mt-1 text-sm font-bold text-white text-right">{{ vinilo.precio }} €</p>
                                </div>
                            </a>
                            <div class="px-4 pb-4 flex items-center gap-2">
                                {% if vinilo.stock > 0 %}
                                    <form method="post" action="{{ path('app_carrito_add') }}" class="flex-1">
                                        <input type="hidden" name="vinilo_id" value="{{ vinilo.id }}">
                                        <input type="hidden" name="_token" value="{{ csrf_token('add-to-cart') }}">
                                        <button type="submit" class="w-full rounded-lg py-2 text-sm font-bold text-white transition-transform hover:scale-105" style="background-color: #e00000;">
                                            Añadir al carrito
                                        </button>
                                    </form>
                                {% else %}
                                    <button disabled class="flex-1 rounded-lg py-2 text-sm font-bold text-gray-800 cursor-not-allowed" style="background-color: #fbbf24;">
                                        Sin stock
                                    </button>
                                {% endif %}
                                <button onclick="toggleFavorito({{ vinilo.id }})" id="fav-btn-{{ vinilo.id }}" class="flex h-9 w-9 items-center justify-center rounded-lg border border-surface-dark {% if vinilo.id in favoritosIds %}bg-[#e00000]{% else %}bg-surface-dark{% endif %} hover:bg-[#e00000] transition-colors">
                                    <span class="material-symbols-outlined text-white text-base">{% if vinilo.id in favoritosIds %}favorite{% else %}favorite_border{% endif %}</span>
                                </button>
                            </div>
                        </article>
                    {% endfor %}
                </div>
            {% else %}
                <div class="flex flex-col items-center justify-center py-24 text-center">
                    <svg fill="currentColor" class="h-16 w-16 text-on-surface-muted-dark mb-4" viewBox="0 0 256 256">
                        <path d="M128,24A104,104,0,1,0,232,128,104.11,104.11,0,0,0,128,24Zm0,192a88,88,0,1,1,88-88A88.1,88.1,0,0,1,128,216Zm40-96a40,40,0,1,1-40-40A40,40,0,0,1,168,120Zm-16,0a24,24,0,1,0-24,24A24,24,0,0,0,152,120Z"/>
                    </svg>
                    <p class="text-on-surface-muted-dark text-lg">No se encontraron vinilos</p>
                    <a href="{{ path('app_vinilo_index') }}" class="mt-4 text-primary hover:underline text-sm">
                        Ver todo el catálogo
                    </a>
                </div>
            {% endif %}
        </section>
    </div>
</main>
{% endblock %}
