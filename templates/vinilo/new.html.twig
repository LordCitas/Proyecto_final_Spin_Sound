{% extends 'base.html.twig' %}

{% block title %}Añadir Vinilo - Spin&Sound{% endblock %}

{% block body %}
<main class="flex-1 animate-reveal">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="max-w-3xl mx-auto">
            <!-- Header -->
            <div class="mb-8">
                <div class="flex items-center gap-3 mb-4">
                    <a href="{{ path('app_admin_panel') }}" class="p-2 hover:bg-surface-dark rounded-lg transition-colors" title="Volver al panel">
                        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="currentColor" viewBox="0 0 256 256">
                            <path d="M224,128a8,8,0,0,1-8,8H59.31l58.35,58.34a8,8,0,0,1-11.32,11.32l-72-72a8,8,0,0,1,0-11.32l72-72a8,8,0,0,1,11.32,11.32L59.31,120H216A8,8,0,0,1,224,128Z"></path>
                        </svg>
                    </a>
                    <h1 class="text-3xl font-bold tracking-tight">Añadir Nuevo Vinilo</h1>
                </div>
                <p class="text-on-surface-muted-dark">Completa los datos del vinilo para añadirlo al catálogo</p>
            </div>

            <!-- Formulario -->
            <div class="bg-surface-dark border border-gray-800 rounded-lg p-8">
                {{ include('vinilo/_form.html.twig') }}
            </div>
        </div>
    </div>
</main>
{% endblock %}
