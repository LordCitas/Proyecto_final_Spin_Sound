<form method="post" action="{{ path('app_vinilo_delete', {'id': vinilo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vinilo.id) }}">
    <button class="btn">Delete</button>
</form>
